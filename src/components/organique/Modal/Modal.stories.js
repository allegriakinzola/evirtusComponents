import Modal from "./Modal";

export default {
    title: "COMPONENTS/organique/Modal",
    component: Modal,
};

const Template = (args) => <Modal {...args} />;

export const Default = Template.bind({});